/**
 * Timezone configuration constants (domain-owned).
 *
 * `config/timezones.js` re-exports this module for legacy compatibility.
 */

const TIMEZONES = {
  DEFAULT: 'America/Argentina/Buenos_Aires',
  ARGENTINA: 'America/Argentina/Buenos_Aires'
};

module.exports = {
  TIMEZONES
};
